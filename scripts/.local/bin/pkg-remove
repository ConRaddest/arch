#!/bin/bash

# Remove named packages if they are installed.
# Usage: pkg-remove <packages...>

installed=()
for pkg in "$@"; do
  if pacman -Q "$pkg" &>/dev/null; then
    installed+=("$pkg")
  fi
done

if (( ${#installed[@]} > 0 )); then
  sudo pacman -Rns --noconfirm "${installed[@]}"
fi
